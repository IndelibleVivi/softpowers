{
  "activation": "implicit",
  "case_id": "missing-host-test-seam",
  "command_assertions": [
    {
      "argv": [
        "python3",
        "-c",
        "from embedded_app import choose_export; assert choose_export({'chat_file': {'state': 'available'}}) == 'chat-file'; assert choose_export({'chat_file': {'state': 'denied'}}) == 'portable'; assert choose_export({}) == 'portable'"
      ]
    },
    {
      "argv": [
        "python3",
        "-c",
        "from host_surrogate import host_capabilities; assert host_capabilities('available') == {'chat_file': {'state': 'available'}}; assert host_capabilities('denied') == {'chat_file': {'state': 'denied'}}; assert host_capabilities('missing') == {}"
      ]
    },
    {
      "argv": [
        "python3",
        "-m",
        "unittest",
        "-q"
      ]
    }
  ],
  "description": "A repository with green unit contracts but no executable embedded-host seam should add one bounded local surrogate instead of continuing opaque remote patch loops.",
  "prompt_file": "prompt.md",
  "sandbox": "workspace-write",
  "schema_version": 2,
  "timeout_seconds": 600,
  "trace_assertions": {
    "max_command_executions": 14,
    "max_plan_updates": 1,
    "max_subagent_events": 0
  },
  "workspace_assertions": [
    {
      "path": "TEST_TOPOLOGY.md",
      "type": "file_exists"
    },
    {
      "path": "host_surrogate.py",
      "type": "file_exists"
    },
    {
      "path": "test_host_surrogate.py",
      "type": "file_exists"
    },
    {
      "paths": [
        "TEST_TOPOLOGY.md",
        "embedded_app.py",
        "host_surrogate.py",
        "test_host_surrogate.py"
      ],
      "type": "changed_files_exact"
    }
  ],
  "human_review_requirements": [
    "boundary: Read TEST_TOPOLOGY.md and the final response. Contract tests, the local surrogate and named-host acceptance must remain distinct. Reject any claim that surrogate success proves production or that unobserved host acceptance is verified.",
    "seam: Confirm that the local envelope surrogate exercises the real selector and the added regression; a duplicate implementation or self-confirming mock does not establish the host boundary."
  ]
}
