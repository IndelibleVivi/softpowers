# Current state

REQ-001 through REQ-003 are implemented and accepted. The storage and CLI contracts
are stable. There is no whole-program implementation plan. Today the owner requests
only the REQ-004 migration/rollback tranche plan, including its REQ-005 verification.
Implementation, redesigning the earlier stages and creating a new total plan are
outside this request. A malformed legacy array must fail without changing either store.
